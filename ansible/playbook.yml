- hosts: databases
  become: true

  vars_files:
    - ../config.yml

  roles:
    - common
    - postgres
    - pgbackrest
    - minio-client

- hosts: blobstores
  become: true

  vars_files:
    - ../config.yml

  roles:
    - common
    - minio
